<h1>Write a new review</h1>

<%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: [0,1,2,3,4,5] %>
    <%= f.submit class: "btn btn-danger"%>
<% end %>

<p><%= link_to "Go back to list", root_path %></p>
